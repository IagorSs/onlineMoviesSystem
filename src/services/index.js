const { novoUsuarioService, getUsuarioService, atualizarUsuario, deletarUsuario } = require("./usuario")

module.exports = {
    novoUsuarioService,
    getUsuarioService,
    atualizarUsuario,
    deletarUsuario,
}